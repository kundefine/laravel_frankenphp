name: Testing Workflow

on:
  workflow_dispatch

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: demo test 1
        run: echo "Running test 1"
      - name: demo test 2
        run: echo "Running test 2"